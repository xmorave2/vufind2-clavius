<?
    // Load standard settings from the default search results screen:
    echo $this->partial('search/results.phtml', $this);

    // Override some details...

    // Set up page title:
    $this->headTitle(
        $this->translate('Author Search Results'),
        Zend_View_Helper_Placeholder_Container_Abstract::SET
    );

    // Set up empty search box:
    $this->layout()->searchbox = $this->partial('search/searchbox.phtml');

    // Set up breadcrumbs:
    $this->layout()->breadcrumbs = '<em>' . $this->results->getDisplayQuery() . '</em>';
?>